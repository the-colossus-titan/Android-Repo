<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_splashscreen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context="com.akshaycodes.todolist.SplashscreenActivity">

   <ImageView
        android:id="@+id/splashscreen_image"
        android:layout_width="wrap_content"
        android:layout_height="350dp"
        android:src="@drawable/todolist_app_icon_2"
        android:layout_centerVertical="true" />
    <TextView
        android:id="@+id/splashText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/splashscreen_image"
        android:layout_alignRight="@id/splashscreen_image"
        android:layout_alignBottom="@id/splashscreen_image"
        android:textSize="27sp"
        android:gravity="center"
        android:textStyle="bold"
        android:textColor="@android:color/holo_blue_light"
        android:text="To Do List"/>


</RelativeLayout>
